// How long each scene lasts in milliseconds
export const SCENE_DURATIONS: Record<string, number> = {
  living: 6000,
  timepass: 8000,
  finalbeat: 4000,
  frozen: 6000,
  collapse: 5000,
  aftermath: 7000,
}

// Beat timing (seconds between beats)
export const BEAT_INTERVAL = 0.75

// How many beats before a crack level advances
export const BEATS_PER_CRACK_LEVEL = 3
